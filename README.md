# re4ctor-public

Public SDK + Gateway/VRF demo stack.

Goals:
- Everything builds and demos without sealed core.
- Sealed core can be plugged in as a backend plugin (RNG_BIN) or private image.

Next:
- docker profiles: public (stub RNG), enterprise (sealed core volume/image)
